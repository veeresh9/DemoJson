const obj = {
    "posts": [
      {
        "title": "Thank You for Choosing our Product"
      }
    ],
    "profile": {
      "name": "typicode"
    }
  }
const res = Object.values(obj).join('\n')

console.log(res)